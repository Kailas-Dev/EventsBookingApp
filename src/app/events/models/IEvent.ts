export interface IEvent {
    name : string,
    topic : string,
    startDate : Date,
    endDate : Date,
    place : string,
    contactPerson : string,
    mobile : string,
    isEventDisabled: boolean
}